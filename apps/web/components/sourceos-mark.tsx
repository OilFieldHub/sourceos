export function SourceOSMark({ className = "" }: { className?: string }) {
  return (
    <span className={className}>
      Source
      <span className="text-[1.2em] font-extrabold">OS</span>
    </span>
  );
}
